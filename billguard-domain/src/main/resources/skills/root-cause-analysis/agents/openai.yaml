interface:
  display_name: "Root Cause Analysis"
  short_description: "Separate bill facts from causal hypotheses"
  default_prompt: "Use $root-cause-analysis to analyze why this charge changed without overstating certainty."
